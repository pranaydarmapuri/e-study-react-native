import useCachedResources from './useCachedResources';
import useColorScheme from './useColorScheme';
import useColorScheme_web from './useColorScheme.web';

export default {
  useColorScheme,
  useCachedResources,
  useColorScheme_web
}